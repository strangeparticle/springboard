This package contains acceptance tests and acceptance-test support.

- Use `acceptance` for CMP UI framework-based tests that model user-visible interaction flows.
- The outer test classes provide the high-level acceptance coverage index.
- Detailed implementation support lives in sibling `*Scenarios.kt` helper files.
- Conventional non-acceptance tests live in the sibling `unit` package.
