This package contains normal unit tests and other non-acceptance test support.

- Use `unit` for conventional Kotlin/unit-style tests of logic, models, utilities, and viewmodels.
- These tests do not use the CMP UI testing framework as their primary mechanism.
- Acceptance-style UI flow coverage lives in the sibling `acceptance` package.
