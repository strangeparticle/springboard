This package contains desktop-specific acceptance tests and acceptance-test support.

- Use `acceptance` here for desktop-only CMP UI acceptance coverage.
- These tests complement shared acceptance coverage in `commonTest` when behavior is platform-specific.
- Conventional desktop unit tests live in the sibling `unit` package.
