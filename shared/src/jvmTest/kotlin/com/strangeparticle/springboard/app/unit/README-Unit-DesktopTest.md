This package contains desktop-specific unit tests and non-acceptance test support.

- Use `unit` here for desktop-only logic tests that do not belong in the shared `commonTest` source set.
- These tests are not the CMP UI acceptance suite.
- Desktop acceptance coverage lives in the sibling `acceptance` package.
